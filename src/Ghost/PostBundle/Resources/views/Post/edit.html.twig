<h1>Post edit</h1>

<form action="{{ path('post_edit', { 'id': post.id }) }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Edit</button>
    </p>
</form>