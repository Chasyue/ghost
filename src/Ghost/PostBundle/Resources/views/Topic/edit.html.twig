<h1>Topic edit</h1>

<form action="{{ path('topic_edit', { 'id': topic.id }) }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Edit</button>
    </p>
</form>